# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'EpiTracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EpiTracker
  pod "LFHeatMap"
  pod 'SPAlert'
  pod 'AlamofireImage'
  pod 'PKHUD'
  pod 'SwiftyJSON'
  
end
